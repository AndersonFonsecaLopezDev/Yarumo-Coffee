'use client'

import { useEffect, useMemo, useState } from 'react'
import { createClient } from '@/lib/supabase/client'

type Request={id:string;type:'waiter'|'bill';status:string;created_at:string;table:{label:string}|null}
type MenuItem={id:string;name:string;slug:string;description:string;price_cop:number;category:string;available:boolean;sort_order:number}
const blank={name:'',slug:'',description:'',price_cop:0,category:'Café',available:true,sort_order:0}

export default function Staff(){
 const supabase=useMemo(()=>createClient(),[]);const [user,setUser]=useState<string|null>(null);const [role,setRole]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [requests,setRequests]=useState<Request[]>([]);const [items,setItems]=useState<MenuItem[]>([]);const [tab,setTab]=useState<'requests'|'menu'>('requests');const [editing,setEditing]=useState<MenuItem|null>(null);const [form,setForm]=useState(blank);const [saving,setSaving]=useState(false)
 async function load(){const {data:{user}}=await supabase.auth.getUser();setUser(user?.email||null);if(!user)return;const profile=await supabase.from('staff_profiles').select('role').eq('user_id',user.id).maybeSingle();setRole(profile.data?.role||'staff');const rq=await supabase.from('service_requests').select('id,type,status,created_at,table:cafe_tables(label)').in('status',['pending','acknowledged']).order('created_at',{ascending:false});setRequests((rq.data||[]) as unknown as Request[]);const menu=await supabase.from('menu_items').select('id,name,slug,description,price_cop,category,available,sort_order').order('sort_order').order('name');setItems((menu.data||[]) as MenuItem[])}
 useEffect(()=>{load();const channel=supabase.channel('staff-requests').on('postgres_changes',{event:'*',schema:'public',table:'service_requests'},load).subscribe();return()=>{supabase.removeChannel(channel)}},[supabase])
 async function login(e:React.FormEvent){e.preventDefault();setError('');const {error}=await supabase.auth.signInWithPassword({email,password});if(error)setError('Correo o contraseña incorrectos.');else load()}
 async function complete(id:string){await supabase.from('service_requests').update({status:'done',completed_at:new Date().toISOString()}).eq('id',id);load()}
 function edit(item:MenuItem){setEditing(item);setForm({...item})} function newItem(){setEditing(null);setForm(blank)}
 async function saveItem(e:React.FormEvent){e.preventDefault();if(!form.name.trim()||form.price_cop<0){setError('Completa un nombre y un precio válido.');return}setSaving(true);setError('');const payload={name:form.name.trim(),slug:form.slug.trim()||form.name.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,''),description:form.description.trim(),price_cop:Number(form.price_cop),category:form.category,available:form.available,sort_order:Number(form.sort_order)||0};const result=editing?await supabase.from('menu_items').update(payload).eq('id',editing.id):await supabase.from('menu_items').insert(payload);setSaving(false);if(result.error){setError(result.error.message.includes('duplicate')?'El slug ya existe. Usa otro.':'No se pudo guardar el producto.');return}newItem();load()}
 async function removeItem(item:MenuItem){if(!confirm(`¿Eliminar ${item.name}?`))return;const {error}=await supabase.from('menu_items').delete().eq('id',item.id);if(error)setError('No se pudo eliminar. Puede haber referencias activas.');else load()}
 if(!user)return <main className="login"><img src="/yarumo-logo.png" alt="Yarumo Coffee" width="72" height="72" style={{borderRadius:'50%'}}/><span className="eyebrow">Acceso del equipo</span><h1>Panel de<br/><em>Yarumo.</em></h1><form onSubmit={login}><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Correo del equipo" required/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Contraseña" required/><button className="button">Entrar al panel →</button></form>{error&&<p>{error}</p>}</main>
 const canManage=role==='owner'||role==='manager';return <main className="staff"><div className="staff-head"><div><span className="eyebrow">Operación en mesa</span><h1>Panel <em>Yarumo.</em></h1></div><button className="button" onClick={()=>supabase.auth.signOut()}>Cerrar sesión</button></div><nav className="staff-tabs"><button className={tab==='requests'?'active':''} onClick={()=>setTab('requests')}>Solicitudes ({requests.length})</button>{canManage&&<button className={tab==='menu'?'active':''} onClick={()=>setTab('menu')}>Menú ({items.length})</button>}</nav>{error&&<p className="admin-error">{error}</p>}{tab==='requests'?<><div className="stats"><div className="stat"><span>Pendientes</span><strong>{requests.length}</strong></div><div className="stat"><span>Mesas activas</span><strong>{new Set(requests.map(r=>r.table?.label)).size}</strong></div><div className="stat"><span>Sesión</span><strong>OK</strong></div></div><div className="request-list">{requests.length?requests.map(r=><article className="request" key={r.id}><div><strong>Mesa {r.table?.label||'—'} · {r.type==='waiter'?'Llamar al mesero':'Pedir la cuenta'}</strong><small>{new Date(r.created_at).toLocaleTimeString('es-CO',{hour:'2-digit',minute:'2-digit'})}</small></div><button className="button" onClick={()=>complete(r.id)}>Atendida</button></article>):<div className="request">No hay solicitudes pendientes.</div>}</div></>:<section className="menu-admin"><div className="menu-admin-head"><div><span className="eyebrow">Catálogo</span><h2>Gestiona la carta.</h2></div><button className="button" onClick={newItem}>+ Nuevo producto</button></div><div className="menu-admin-grid"><div className="admin-list">{items.map(item=><article className={`admin-item ${!item.available?'unavailable':''}`} key={item.id}><div><strong>{item.name}</strong><small>{item.category} · ${item.price_cop.toLocaleString('es-CO')} · {item.available?'Visible':'Oculto'}</small></div><div><button onClick={()=>edit(item)}>Editar</button><button onClick={()=>removeItem(item)}>Eliminar</button></div></article>)}</div><form className="menu-form" onSubmit={saveItem}><h3>{editing?'Editar producto':'Nuevo producto'}</h3><input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Nombre" required/><input value={form.slug} onChange={e=>setForm({...form,slug:e.target.value})} placeholder="slug-opcional"/><textarea value={form.description} onChange={e=>setForm({...form,description:e.target.value})} placeholder="Descripción" maxLength={500}/><input type="number" min="0" value={form.price_cop} onChange={e=>setForm({...form,price_cop:Number(e.target.value)})} placeholder="Precio en pesos colombianos" required/><select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option>Café</option><option>Frío</option><option>Para comer</option><option>Otros</option></select><input type="number" min="0" value={form.sort_order} onChange={e=>setForm({...form,sort_order:Number(e.target.value)})} placeholder="Orden"/><label><input type="checkbox" checked={form.available} onChange={e=>setForm({...form,available:e.target.checked})}/> Visible en la carta</label><div><button type="submit" className="button" disabled={saving}>{saving?'Guardando…':'Guardar producto'}</button>{editing&&<button type="button" onClick={newItem}>Cancelar</button>}</div></form></div></section>}</main>
}
