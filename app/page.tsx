'use client'

import { useEffect, useMemo, useState } from 'react'
import { createClient } from '@/lib/supabase/client'

type MenuItem={id:string;name:string;description:string;price_cop:number;category:string;sort_order:number;image_url:string|null}

export default function Home(){
  const supabase=useMemo(()=>createClient(),[]);const [table,setTable]=useState<{id:string,label:string}|null>(null);const [menu,setMenu]=useState<MenuItem[]>([]);const [notice,setNotice]=useState('');const [loading,setLoading]=useState(false);const [query,setQuery]=useState('');const [category,setCategory]=useState('Todos')
  useEffect(()=>{supabase.from('menu_items').select('id,name,description,price_cop,category,sort_order,image_url').eq('available',true).order('sort_order').order('name').then(({data})=>setMenu((data||[]) as MenuItem[]));const token=new URLSearchParams(window.location.search).get('mesa');if(!token)return;supabase.rpc('get_table_by_token',{token}).then(({data})=>{if(data?.[0])setTable(data[0]);else setNotice('Este QR no está activo. Pide ayuda al equipo.')})},[supabase])
  async function request(type:'waiter'|'bill'){if(!table){setNotice('Escanea el QR asignado a tu mesa para solicitar atención.');return}setLoading(true);const {error}=await supabase.from('service_requests').insert({table_id:table.id,table_token:new URLSearchParams(window.location.search).get('mesa'),type});setLoading(false);setNotice(error?'No pudimos enviar la solicitud. Intenta de nuevo.':type==='waiter'?'El mesero recibió tu solicitud.':'La cuenta fue solicitada para tu mesa.')}
  const categories=['Todos',...Array.from(new Set(menu.map(item=>item.category)))];const filtered=menu.filter(item=>(category==='Todos'||item.category===category)&&`${item.name} ${item.description}`.toLowerCase().includes(query.toLowerCase()));return <><header className="header"><a className="brand" href="#inicio"><img src="/yarumo-logo.png" alt="Yarumo Coffee"/><span><strong>Yarumo</strong><small>COFFEE</small></span></a><nav className="nav-links"><a href="#menu">Carta</a><a href="#visitanos">Visítanos</a></nav><a className="instagram-link" href="https://www.instagram.com/yarumocafearmenia/" target="_blank" rel="noreferrer">Instagram ↗</a></header><main id="inicio"><section className="hero"><div><span className="eyebrow">Café de origen · Armenia</span><h1>Más que café,<br/><em>momentos.</em></h1><p>Abre tu carta, pide la cuenta o llama al mesero sin levantarte de la mesa.</p><a className="hero-link" href="#menu">Explorar la carta <span>↓</span></a></div><div className="hero-photo"><img src="/yarumo-cover-cafe.png" alt="Café de Yarumo Coffee servido en mesa"/><div className="hero-photo-caption"><span>{table?`Mesa ${table.label}`:'Yarumo Coffee'}</span><strong>Hecho para<br/><em>quedarse un rato.</em></strong></div></div></section><section className="service"><div className="service-card"><div><span className="eyebrow">{table?`Mesa ${table.label}`:'Servicio en mesa'}</span><h2>Todo desde tu celular.</h2><p>Sin esperas. Toca una opción y el equipo recibe la solicitud.</p></div><div className="service-actions"><button className="primary" onClick={()=>request('waiter')} disabled={loading}>♧ <span>{loading?'Enviando…':'Llamar al mesero'}</span></button><button onClick={()=>request('bill')} disabled={loading}>▱ <span>Pedir la cuenta</span></button></div></div></section><section className="content" id="menu"><div className="section-top"><div><span className="eyebrow">La carta</span><h2>Elige tu momento.</h2></div><div className="search"><span>⌕</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar en la carta" aria-label="Buscar en la carta"/></div></div><div className="category-tabs">{categories.map(item=><button key={item} className={category===item?'active':''} onClick={()=>setCategory(item)}>{item}</button>)}</div><div className="menu">{filtered.length?filtered.map(item=><article className="item" key={item.id}>{item.image_url?<img className="item-image" src={item.image_url} alt={item.name} loading="lazy" onError={e=>{e.currentTarget.style.display='none'}}/>:<div className="item-placeholder">Y</div>}<div className="item-copy"><span>{item.category}</span><h3>{item.name}</h3><p>{item.description}</p><small>{new Intl.NumberFormat('es-CO',{style:'currency',currency:'COP',maximumFractionDigits:0}).format(item.price_cop)}</small></div></article>):<div className="empty-state">No encontramos ese antojo.<br/><button onClick={()=>{setQuery('');setCategory('Todos')}}>Ver toda la carta</button></div>}</div></section><section className="visit-strip" id="visitanos"><div><span className="eyebrow">Ven a vernos</span><h2>Tu mesa<br/><em>te espera.</em></h2></div><div><p>Carrera 19 # 21 Norte-01<br/>Armenia, Colombia</p><a href="https://maps.app.goo.gl/yr8DTNsgNKm6NQb6A?g_st=iw" target="_blank" rel="noreferrer">Abrir ubicación ↗</a></div></section><footer className="footer"><span>Yarumo Coffee · Hecho para quedarse un rato.</span><span><a href="https://www.google.com/search?q=Yarumo+Coffee+Armenia" target="_blank" rel="noreferrer">Reseñas de Google ↗</a> · <a href="https://www.instagram.com/yarumocafearmenia/" target="_blank" rel="noreferrer">Redes ↗</a></span></footer></main>{notice&&<button className="notice" onClick={()=>setNotice('')}>{notice} ×</button>}</>
}
